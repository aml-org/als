asyncapi: 2.0.0
channels:
  user/{paramName}/signedup:
    parameters:
      paramName:
        schema:
            type: object
            properties:
                n:
                    type: string