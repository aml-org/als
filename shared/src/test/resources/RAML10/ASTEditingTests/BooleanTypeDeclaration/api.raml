#%RAML 1.0

title: test API
mediaType: application/json
baseUri: www.{domain}/{airplane}
baseUriParameters:
  airplane:
    type: boolean
    enum: [true]
types:
  airport:
    type: boolean
    enum: [true]
annotationTypes:
  airport:
    type: boolean
    enum: [true]
/resource/{airplane}:
  uriParameters:
    airplane:
      type: boolean
      enum: [true]
  post:
    queryParameters:
      airplane:
        type: boolean
        enum: [true]
    headers:
      A-IM:
        type: boolean
        enum: [true]
    body:
      type: boolean
      enum: [true]
    responses:
      201:
        body: airport
