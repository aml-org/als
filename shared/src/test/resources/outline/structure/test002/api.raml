#%RAML 1.0
title: API
resourceTypes:
  main:
    get:
      responses:
        200:
          body:
            application/json:
              description: some very useful resource
baseUri: /some/base/uri

/someChildUri:
  type: main
  post:
    responses:
      200:
        body:
          application/xml:
            description: some very useful resource
  /anotherChild:
    put:
      body:
        application/json:
          description: another very useful resource
