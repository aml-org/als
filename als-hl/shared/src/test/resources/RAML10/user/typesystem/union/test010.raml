#%RAML 1.0
title: API with Types
types:
  union:
    type: object | CustomDate
  PossibleMeetingDate:
    type: CustomDate
    noHolidays: true
  CustomDate:
    type: date-only
    facets:
      onlyFutureDates?: boolean # optional  in `PossibleMeetingDate`
      noHolidays: boolean # required in `PossibleMeetingDate`
