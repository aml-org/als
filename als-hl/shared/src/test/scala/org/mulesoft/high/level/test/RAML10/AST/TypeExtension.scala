package org.mulesoft.high.level.test.RAML10.AST

import org.mulesoft.high.level.test.RAML10.RAML10ASTTest

class TypeExtension extends RAML10ASTTest {

//  test("TypeExtension name") {
//    runTest("ASTTests/TypeExtension/TypeExtension.raml", project => {
//
//      var expectedValue = "objectName"
//      var length = project.rootASTUnit.rootNode.elements("types").find(t=> t.attribute("name").get.value.contains("airport")).get.attribute("name").get.value
//      if (length == Some(expectedValue))
//        succeed
//      else
//        fail(s"Expected value: $expectedValue, actual: ${length}")
//    })
//  }
//
//  test("TypeExtension value") {
//    runTest("ASTTests/TypeExtension/TypeExtension.raml", project => {
//
//      var expectedValue = "objectName"
//      var length = project.rootASTUnit.rootNode.elements("resources").head.element("type").head.elements("parameters").head.attribute("value").get.value
//      if (length == Some(expectedValue))
//        succeed
//      else
//        fail(s"Expected value: $expectedValue, actual: ${length}")
//    })
//  }
}
