asyncapi: 2.0.0
components:
  securitySchemes:
    oauth1:
      type: oauth1
      description: oauth1
      name: key
      scheme: invalid
      bearerFormat: none
    oauth2:


servers:
  someServer:
    security:
      - *