#%AsyncAPI 2.0

asyncapi: 2.0.0
components:
  serverBindings:
    serverBinding:
      http:
  channelBindings:
    channelBinding:
      http:
  operationBindings:
    operationBinding:
      http:
  messageBindings:
    messageBinding:
      http:

servers:
  server:
    $ref: *