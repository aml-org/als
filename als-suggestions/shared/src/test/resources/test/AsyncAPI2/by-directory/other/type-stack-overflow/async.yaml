asyncapi: "2.0.0"

components:
  schemas:
    A:
      type: string

channels:
  users/signup:
    subscribe:
      message:
        # schemaFormat: application/async+yaml;version=1.0
        payload:
          type: object
          properties:
            a:
              type: *