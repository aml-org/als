asyncapi: 2.0.0
components:
  serverBindings:
    serverBinding:
      http:
  channelBindings:
    channelBinding:
      http:
  operationBindings:
    operationBinding:
      http:
  messageBindings:
    messageBinding:
      http:

channels:
  user/{paramName}/signedup:
    bindings:
      $ref: *