openapi: 3.0.0

paths:
  /users:
    post:
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/User'
      responses:
        '201':
          links:
            GetUserByUserId:
              parameters:
                userId: '$response.*'