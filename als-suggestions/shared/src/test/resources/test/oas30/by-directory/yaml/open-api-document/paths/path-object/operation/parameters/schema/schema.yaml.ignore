openapi: 3.0.0
paths:
  /path:
    get:
      parameters:
        - schema:
            type: object
            properties:
              name: string
              age: integer
            discriminator:
              *