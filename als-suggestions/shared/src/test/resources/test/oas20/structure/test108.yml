---
swagger: '2.0'
info:
  title: api
  description: api
  version: 1.0

host: api.example.com
basePath: /v1

schemes:
  - https

definitions:
  User:
    properties:
      name:
        type: number
      id:
        type: string
        enum: [aa, bb]

    required:
      - *
