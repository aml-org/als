swagger: '2.0'
info:
  title: t
  version: '1'

securityDefinitions:
  internalApiKey:
    type: apiKey
    tokenUrl: something

paths:
  "/path":
    get:
      responses:
        "200":
          description: "Something"
      security: *