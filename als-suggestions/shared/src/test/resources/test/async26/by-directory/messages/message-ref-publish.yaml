asyncapi: 2.6.0
channels:
  eastern-books:
    publish:
      message:
        $ref: *
components:
  messages:
    test:
      payload:
        type: object
        properties:
          id:
            type: string
