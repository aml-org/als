swagger: '2.0'
definitions:
  RENAMED:
    properties:
      name:
        type: string
paths:
  /users/{userId}:
    get:
      responses:
        200:
          schema:
            $ref: '#/definitions/RENAMED'
        404:
          schema:
            $ref: '#/definitions/RENAMED'