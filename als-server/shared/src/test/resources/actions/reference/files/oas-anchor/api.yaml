swagger: '2.0'
info:
  title: titulo
  version: 2

definitions:
  User:
    pr[*]operties:
      name:
        type: string

paths:
  /users/{userId}:
    get:
      responses:
        200:
          schema:
            $ref: '#/definitions/User'