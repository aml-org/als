openapi: 3.0.0

info:
  title: test
  version: 1.0.0

paths:
  /p:
    get:
      responses:
        "200":
          description: desc
      parameters:
        - schema:
            type: string
            example: t[*]extplain