openapi: "3.0.0"
info:
  title: asd
  version: 2
components:
  call[*]backs:
    call:
      endpoint:
        description:
          asd
  e[*]xamples:
    ex:
      description: desc
  heade[*]rs:
    myHeader:
  li[*]nks:
    aLink:
      description: desc
  par[*]ameters:
    param:
      description: desc
  requ[*]estBodies:
  resp[*]onses:
  sc[*]hemas:
  secu[*]ritySchemes:
