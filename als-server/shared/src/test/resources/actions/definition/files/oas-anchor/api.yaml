swagger: '2.0'
info:
  title: titulo
  version: 2

definitions:
  User:
    properties:
      name:
        type: string

paths:
  /users/{userId}:
    get:
      responses:
        200:
          schema:
            $ref: '#/defin[*]itions/User'