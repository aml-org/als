export declare function findSelection(node: any, position: any, content: any): string;
export declare function reduce(fullContent: string, selection: string, range: {
    start: number;
    end: number;
}): {
    start: number;
    end: number;
};
