asyncapi: 2.0.0

components:
  schemas:
    t_string:
      type: string
channels:
  user/signedup:
    subscribe:
      message:
        schemaFormat: application/raml+yaml;version=1.0
        payload:
          type: object
          properties:
            x: string